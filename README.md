# de-aws-password-manager
The command-line app to interact with AWS SM service,
app takes communicates with user and communicates to AWS service using boto3 to store user's input in the AWS Secrets Manager
